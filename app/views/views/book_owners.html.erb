<h1>Proprietários dos livros</h1>

<table>

  <tr>
    <th>Nome</th>
    <th>Proprietário</th>
  </tr>

  <% @books.each do |book| %>
      <tr>
        <td><%= book.name %></td>
        <td><%= book.nomeusers %></td>
      </tr>
  <% end %>

</table>