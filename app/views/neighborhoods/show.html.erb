<%= render 'layouts/show_header' %>

<%= render 'layouts/flash_success' %>

<div class="row">

  <div class="col-sm-12">

    <div class="well">

      <div>
        <strong>Name:</strong>
        <%= @neighborhood.name %>
      </div>

    </div>

  </div>

</div>

<div class="row">
  <div class="col-sm-12">
    <%= link_to 'Streets', neighborhood_streets_path(@neighborhood), class: 'btn btn-primary btn-xs' %>
    <%= link_to 'Edit', edit_neighborhood_path(@neighborhood), class: 'btn btn-primary btn-xs' %>
    <%= link_to 'Back', city_neighborhoods_path(@neighborhood.city), class: 'btn btn-warning btn-xs' %>
  </div>
</div>