<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @street.name %>
</p>

<p>
  <strong>Kind:</strong>
  <%= @street.kind %>
</p>

<div>
  <%= link_to 'Address', street_addresses_path(@street) %>
</div>
<%= link_to 'Edit', edit_street_path(@street) %> |
<%= link_to 'Back', neighborhood_streets_path(@street.neighborhood) %>
