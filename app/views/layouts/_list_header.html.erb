<div class="row">
  <div class="col-sm-12">
    <div class="page-header">
      <% extra_title ||= '' %>
      <h2>Listing <%= params[:controller].humanize %> <%= extra_title %> <%= link_to "New #{params[:controller].singularize.humanize}", url_for(controller: params[:controller], action: :new), class: 'btn btn-primary btn-xs' %></h2>
    </div>
  </div>
</div>