<%= render 'layouts/list_header_title', title: 'Top exchanged titles' %>

<%= render 'layouts/flash_success' %>

<div class="row">

  <div class="col-sm-12">

    <div class="table-responsive">

      <table class="table table-bordered table-hover">

        <tr>
          <th>Nome</th>
          <th>Trocas</th>
        </tr>

        <% @books.each do |book| %>
            <tr>
              <td><%= book.name %></td>
              <td><%= book.exchanges %></td>
            </tr>
        <% end %>

      </table>

    </div>

  </div>

</div>